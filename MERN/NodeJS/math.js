let sum = (a,b) =>{
    return a+b;
}

let mul = (a,b) => {
    return a*b;
}

let Pi = 3.14;
let area = 10;

let obj = {
    sum : sum,
    mul : mul,
    Pi : Pi,
    Area : area
}

module.exports = obj;