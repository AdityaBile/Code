<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit message</title>
  </head>
  <body>
    <h4>This message was send by <%= currChat.from %> to <%= currChat.to %></h4>
    <form method="POST" action="/chats/<%= currChat._id%>?_method=PUT">
      <textarea name="msg" cols="30" rows="5"><%= currChat.msg %></textarea>
      <br />
      <button>Submit</button>
    </form>
  </body>
</html>
